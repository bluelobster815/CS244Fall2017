.pioenvs
.piolibdeps
.vscode/c_cpp_properties.json
# Janus: added this as it contains a hardcoded path on my machine.
# However, not sure if this file can be autogenerated w/o re-initializing project so we may need to add it to VC at a later stage.
.vscode/launch.json

# Ignore the Config file as it contains credentials and other secrets.
src/Config.h
